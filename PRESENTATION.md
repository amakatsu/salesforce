# PR-Agent 導入提案

本日は、GitLab のマージリクエストを自動レビューし、レビュアーの負担を軽減しつつ品質を標準化するシステムについてご説明します。

---

## 🧭 要約

まず、要約からお話しします。

### 何をしたか

PR-Agent という OSS をカスタムして、マージリクエストのレビューアプリを構築しました。

具体的には、3 つの層を独自実装しています。

1 つ目は、カスタム LLM ハンドラーです。これにより、社内の Ai-bow API に対応しています。

2 つ目は、Web UI です。Streamlit で構築し、検証フェーズで設定を調整できるようにしています。

3 つ目は、TOML 設定管理です。ドメイン別のレビュー基準を柔軟に管理できます。

### 今後何をしたいか

今後の展開は 2 つあります。

1 つ目は、Webhook 自動実行への移行です。全てのマージリクエストを自動レビューできるようにします。

2 つ目は、設定ファイルのブラッシュアップです。Java、Salesforce、DTO などのレビュー観点を精緻化していきます。

---

## 📖 PR-Agent とは？

### OSS の概要

PR-Agent は、コードレビューを自動化する OSS です。

主な機能は 4 つあります。

1 つ目は `/review` コマンドで、コードレビューを実行します。セキュリティ、パフォーマンス、テスト、設計などの観点で自動レビューし、問題点や改善提案をマージリクエストにコメント投稿します。

2 つ目は `/improve` コマンドで、コード改善提案を行います。より良い実装方法の提案や、バグの匂いを指摘します。

3 つ目は `/describe` コマンドで、マージリクエストの説明文を自動生成します。変更内容の要約や、変更理由、影響範囲を説明してくれます。

4 つ目は `/ask` コマンドで、コードに関する質問に回答します。

### 通常の利用形態

通常は、Webhook 連携でマージリクエスト作成や更新時に自動実行するか、CLI から手動実行します。

### 設定ファイルでカスタマイズ可能

PR-Agent は TOML ファイルでカスタムプロンプトを指定できます。プロジェクト特化のレビュー観点を追加できるため、例えば Java のコーディング規約、Salesforce のコーディング規約、DTO パターンの確認などを設定できます。

### PR-Agent の内部構成

PR-Agent は Python ベースのツールで、以下の構成になっています。

まず、コマンド実行層があり、review、improve、describe、ask などのコマンドを実行します。

次に、コンテキスト層があり、設定ファイルを TOML で管理します。

そして、LiteLLM という LLM 抽象化層があります。ここが重要なポイントで、OpenAI、Gemini、Claude には対応していますが、Ai-bow API には対応していません。

最後に、Git 連携層があり、GitLab API でマージリクエストを取得したり、コメントを投稿したりします。

---

## 🎯 独自実装とカスタマイズ

### 3 つの課題

PR-Agent をそのまま使うには、3 つの課題がありました。

1 つ目の課題は、Ai-bow API が使えないことです。LiteLLM は外部 LLM サービス、つまり OpenAI、Gemini、Claude には対応していますが、社内の Ai-bow API には非対応です。

2 つ目の課題は、いきなり Webhook 運用はハードルが高いことです。いきなり Hook での運用にすると検証できず、手戻りの可能性があります。

3 つ目の課題は、ドメイン特化のレビュー基準が必要なことです。Java、Salesforce、DTO など、プロジェクトごとに異なるレビュー観点を検証しながら調整する必要があります。

### 解決策：3 層の独自実装

これらの課題に対応するため、3 層の独自実装を行いました。

1 つ目の課題、LLM 対応については、カスタム LLM ハンドラーを実装しました。LLM ハンドラーをオーバーライドして、Ai-bow API に対応させています。

2 つ目の課題、検証環境については、Streamlit で Web UI を構築しました。手動実行やログ確認で安全に検証できます。

3 つ目の課題、設定管理については、TOML 設定ファイル管理を実装しました。Web UI から選択・編集し、レビュー基準を検証しながら調整可能です。

---

## 💻 準備したもの

上記の独自実装を組み込んで、以下のシステムを構築しました。

ユーザーはブラウザから Web UI にアクセスします。Web UI では、マージリクエストの URL 入力、設定編集、ログ表示ができます。

次に、カスタム LLM ハンドラーが動作します。ここで、LLM ハンドラーをオーバーライドし、Ai-bow API を呼び出して、レスポンスを変換します。

そして、PR-Agent が動作します。ここは OSS を活用している部分で、LiteLLM、GitLab 連携、レビューロジックなどが含まれます。

最後に、GitLab とやり取りします。

ポイントは、カスタム LLM ハンドラーが Ai-bow API と PR-Agent の橋渡し役になっていることです。ユーザーは Web UI から手動で検証可能です。

実際の画面としては、PR-Agent 実行画面、実行ログと結果表示、設定ファイル編集画面の 3 つを用意しています。

---

## 🔄 Webhook 自動実行フロー（将来像）

将来的には、Webhook で自動実行するフローを実現します。

GitLab でマージリクエストが作成または更新されると、Webhook がイベントを受け取ります。

Webhook はバックエンドに設定を適用し、バックエンドは PR-Agent にレビュー実行を指示します。

PR-Agent は GitLab からマージリクエストの差分を取得し、LLM ハンドラーにレビュー要求を送ります。

LLM ハンドラーは Ai-bow API を呼び出し、レビュー結果を受け取ります。

そして、レビュー結果を PR-Agent に返却し、PR-Agent が GitLab にコメントを投稿します。

このフローにより、完全自動でレビューが実施されます。

---

## 💡 MCP との違い

ここで、よくある質問にお答えします。「MCP での都度コメントではダメなのか」という点です。

今回の目的は「マージリクエストレビューの自動化・標準化」です。MCP は以下の理由で適していません。

1 つ目の理由は、対話型のため自動化できないことです。MCP はユーザーが毎回指示を出す必要がありますが、私たちの目標は Webhook で全マージリクエストを自動レビューすることです。

2 つ目の理由は、個人の開発環境に閉じることです。MCP は各開発者のローカル環境で動作しますが、私たちの目標はチーム全体で統一基準のレビューを実現することです。

3 つ目の理由は、タイミングが異なることです。MCP はコード作成中、つまり Before で使いますが、私たちの目標はマージリクエスト作成後のレビュー、つまり After です。

比較表で整理すると、目的、実行タイミング、自動化、適用範囲、レビュー標準化の全ての観点で、MCP と PR-Agent は異なります。

---

## 🔄 段階的導入計画

導入は段階的に進めます。

Phase 1 は、現在の検証フェーズです。Web UI で手動実行し、レビュー結果を確認・評価して、設定ファイルをブラッシュアップしていきます。

Phase 2 は、将来の運用フェーズです。Webhook で全てのマージリクエストを自動レビューし、マージリクエスト作成時に自動的にレビューコメントを投稿します。これにより、レビュアーは高難度領域に集中できるようになります。

---

## まとめ

最後にまとめです。

このシステムで実現することは 3 つあります。レビュー待ち時間の短縮、レビュー観点の標準化、そしてレビュアーの負荷軽減です。

導入は段階的に進めます。現在は Web UI で検証を行い、将来的には Webhook で全マージリクエストの自動レビューを実現します。

技術的には 3 つの工夫をしています。Ai-bow API 対応のカスタムハンドラー、ドメイン別設定ファイルによる柔軟なカスタマイズ、そして Web UI による安全な検証です。

以上により、チーム全体のレビュー品質向上と効率化を実現します。

ご清聴ありがとうございました。

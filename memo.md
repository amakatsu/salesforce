以下、新人向けに「何をするか」＋「なぜそうするか」をセットでまとめます。

---

## ① パラメータは boolean・任意・未指定は default（例：false）

### 何をする？

* `includeDeleted` は **true/false のスイッチ**にする
* **任意（省略OK）**にする
* 省略されたら **サーバ側で false 扱い**にする
* OpenAPI にも **default: false** を書く

**呼び出し例**

* 通常：`GET /resources`（＝削除は含めない）
* 削除も含める：`GET /resources?includeDeleted=true`

**OpenAPI例**

```yaml
- name: includeDeleted
  in: query
  required: false
  schema:
    type: boolean
    default: false
  description: true のとき削除データも含める（省略時は false）
```

### なぜそうする？

* **普段の呼び出しが楽**：よくあるケース（削除を含めない）が `GET /resources` だけで済む
* **意図が明確**：true/false で意味がブレない（「1」「0」「yes」みたいな曖昧さが無い）
* **後方互換が強い**：新しい任意パラメータを追加しても既存クライアントが壊れない
* **仕様が伝わる**：OpenAPIにdefaultを書いておくと「未指定時どうなる？」が一目で分かる
  （※サーバ実装でも必ず default を適用する。OpenAPIだけだと自動適用されないことがあるため）

---

## ② 0件でも例外にせず、空配列 `[]` を返す（200 OK）

### 何をする？

* 検索/一覧は **0件でも正常**として扱う
* **200 OK** を返す
* 配列は **空配列 `[]`** を返す（nullにしない）

**レスポンス例（0件）**

```json
{
  "resources": [],
  "total": 0,
  "limit": 50,
  "offset": 0
}
```

### なぜそうする？

* **「条件に合うデータが無い」はエラーではない**
  （APIは正常に検索できている。結果が0件なだけ）
* **クライアントが実装しやすい**

  * 常に `for (r of resources)` で回せる（0件でも動く）
  * `null` だと毎回 `if (resources != null)` が必要になりバグが増える
* **ページング/件数表示が自然**
  `total: 0` を返せば UI で「0件」と素直に出せる
* **HTTPの意味に合う**

  * 一覧で 404 にすると「エンドポイントが存在しない」みたいに誤解される
  * 204 にするとボディが返せず、ページング情報などが返しにくい

---

## 新人向けの覚え方（超短い）

* **パラメータ**：送らなければ “いつも通り”（default）
* **検索結果**：無ければ “空っぽ”（`[]`）、エラーじゃない

---

必要なら、あなたのAPI仕様書にそのまま貼れる「共通ルール文（パラメータ未指定時／0件時）」を日本語で整形して渡します。

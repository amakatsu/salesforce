# Streamlit - Webフレームワーク
streamlit>=1.28.0

# word.pyの依存パッケージ
pandas>=2.0.0
openpyxl>=3.1.0
requests>=2.31.0
python-dotenv>=1.0.0
certifi>=2023.7.22
rapidfuzz>=3.0.0

# domain_check.pyの依存パッケージ
# (pandas, openpyxl, requests は上記で含まれている)

# PR-Agent
# pr-agent-pro>=0.2.0
toml>=0.10.2

# Windows互換（必要に応じて）
# win_inet_pton>=1.1.0; sys_platform == 'win32'
